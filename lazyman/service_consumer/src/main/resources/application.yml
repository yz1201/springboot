server:
  port: 8888
spring:
  application:
    name: service-consumer
eureka:
  client:
    service-url:
      defaultZone: http://localhost/eureka
    fetch-registry: true
    registry-fetch-interval-seconds: 10
